# Keep LF in the working tree everywhere. Without this, a Windows checkout with
# core.autocrlf=true rewrites weights/SHA256SUMS with CRLF, and `sha256sum -c` then
# fails to find the file it names.
* text=auto eol=lf

weights/SHA256SUMS text eol=lf
*.pt  binary
*.png binary
